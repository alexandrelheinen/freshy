/** Map search radius limits for explore and list views. */
export const MAP_SEARCH = {
  /** Maximum search radius in km (prevents unbounded queries at low zoom). */
  maxRadiusKm: 100,
  /** Minimum search radius in km. */
  minRadiusKm: 0.1,
};
