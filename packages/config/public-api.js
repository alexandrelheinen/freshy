/** Production Freshy API Worker URL (no trailing slash). */
export const PRODUCTION_API_URL = 'https://freshy-api.alexandrelheinen.workers.dev';

/** Local wrangler dev default (see docs/local-development.md). */
export const LOCAL_API_URL = 'http://localhost:8787';
