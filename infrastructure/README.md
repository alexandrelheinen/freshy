# Infrastructure

| Path | Description |
|------|-------------|
| `docker/` | Local PostgreSQL + PostGIS (`docker-compose.yml`) |
| `gcp/` | Google Cloud Storage setup for assets & CI screenshots |

```bash
bash scripts/setup-local-db.sh
```

See [gcp/README.md](gcp/README.md) for bucket configuration.
