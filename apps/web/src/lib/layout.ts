/** Matches `h-16` on AppTopNav / AppMobileHeader. */
export const APP_HEADER_HEIGHT_PX = 64;

/** Matches the mobile add-place fixed footer (button + padding). */
export const MOBILE_FORM_FOOTER_PADDING_CLASS = 'pb-28';
